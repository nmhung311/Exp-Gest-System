#!/usr/bin/env node

// Script để set production environment variables
// Sử dụng: node set-production-env.js <your-domain.com>

const fs = require('fs')
const path = require('path')

const domain = process.argv[2]

if (!domain) {
  console.error('❌ Vui lòng cung cấp domain!')
  console.log('📝 Cách sử dụng: node set-production-env.js <your-domain.com>')
  console.log('📝 Ví dụ: node set-production-env.js myapp.vercel.app')
  process.exit(1)
}

// Tạo nội dung cho .env.production
const envContent = `# Production environment variables
# Generated by set-production-env.js
NEXT_PUBLIC_API_BASE_URL=https://${domain}
NEXT_PUBLIC_FRONTEND_URL=https://${domain}
`

// Tạo nội dung cho .env.local (development)
const devEnvContent = `# Development environment variables
# This file is for local development only
NEXT_PUBLIC_API_BASE_URL=http://localhost:3000
NEXT_PUBLIC_FRONTEND_URL=http://localhost:3000
`

// Ghi file .env.production
fs.writeFileSync(path.join(__dirname, '.env.production'), envContent)
console.log('✅ Đã tạo file .env.production với domain:', domain)

// Ghi file .env.local nếu chưa có
const envLocalPath = path.join(__dirname, '.env.local')
if (!fs.existsSync(envLocalPath)) {
  fs.writeFileSync(envLocalPath, devEnvContent)
  console.log('✅ Đã tạo file .env.local cho development')
}

// Cập nhật config.ts với domain mới
const configPath = path.join(__dirname, 'lib', 'config.ts')
let configContent = fs.readFileSync(configPath, 'utf8')

// Thay thế domain trong config
configContent = configContent.replace(
  /'https:\/\/your-domain\.com'/g,
  `'https://${domain}'`
)

fs.writeFileSync(configPath, configContent)
console.log('✅ Đã cập nhật config.ts với domain:', domain)

console.log('\n🎉 Hoàn thành! Bây giờ bạn có thể:')
console.log('📝 Development: npm run dev')
console.log('📝 Production: npm run build')
console.log(`🌐 Domain: https://${domain}`)
